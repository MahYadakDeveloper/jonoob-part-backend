{
  "extends": "./tsconfig.json",
  "include": [
    "api/src/**/*.ts",
    "src/**/*.ts",
    "test/**/*.ts",
    // "src/**/*.spec.ts",
    "typings/**/*.ts",
    // etc

    // if you have a mixed JS/TS codebase, don't forget to include your JS files
    "src/**/*.js"
  ]
}
